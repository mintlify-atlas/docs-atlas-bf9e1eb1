---
title: "CoworkingBooking API: Space Reservation REST Service"
sidebarTitle: "Home"
description: "CoworkingBooking is a Spring Boot REST API for managing coworking spaces, users, and bookings. Explore endpoints, data models, and integration guides."
---

CoworkingBooking is a Spring Boot 4 REST API that powers coworking space management — handling spaces (espacios), users (usuarios), and reservations (reservas) backed by PostgreSQL. The API exposes clean JSON endpoints for every CRUD operation and ships with an OpenAPI spec at `/api-docs`.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Make your first API call and create a reservation in under five minutes.
  </Card>
  <Card title="Configuration" icon="gear" href="/configuration">
    Set up your database, environment variables, and CORS settings.
  </Card>
  <Card title="Data Model" icon="database" href="/concepts/data-model">
    Understand the three core entities: Espacio, Usuario, and Reserva.
  </Card>
  <Card title="API Reference" icon="code" href="/api/espacios/overview">
    Full endpoint documentation for all three resources.
  </Card>
</CardGroup>

## What the API provides

CoworkingBooking exposes three resource groups — each with a complete set of HTTP endpoints:

<CardGroup cols={3}>
  <Card title="Espacios" icon="building" href="/api/espacios/overview">
    Create and manage physical coworking spaces with capacity and cleaning time.
  </Card>
  <Card title="Reservas" icon="calendar" href="/api/reservas/overview">
    Book spaces with start/end timestamps and track reservation status.
  </Card>
  <Card title="Usuarios" icon="user" href="/api/usuarios/overview">
    Register users and manage access with role-based records.
  </Card>
</CardGroup>

## Get started in three steps

<Steps>
  <Step title="Run the server">
    Clone the repo, configure environment variables, and start the Spring Boot application with `./mvnw spring-boot:run`.
  </Step>
  <Step title="Create a user and a space">
    POST to `/usuarios/create` and `/espacios/create` to seed the entities you need before making a reservation.
  </Step>
  <Step title="Book a space">
    POST to `/reserva/create` with the user ID, space ID, and your desired time window. The API returns a full reservation object with status.
  </Step>
</Steps>

<Note>
  The OpenAPI spec is served at `/api-docs` and an interactive Swagger UI is available at `/swagger-ui.html` when the server is running.
</Note>
